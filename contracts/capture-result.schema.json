{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.local/chipk-capture/result-v1.json",
  "title": "ChipK Capture Result v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["contractVersion", "requestId", "provider", "status", "artifacts", "evidence"],
  "properties": {
    "contractVersion": { "const": 1 },
    "requestId": { "type": "string", "minLength": 1 },
    "provider": {
      "type": "object",
      "required": ["id", "toolVersion"],
      "properties": {
        "id": { "const": "chipk-simulator-capture" },
        "toolVersion": { "type": "string" }
      }
    },
    "status": { "enum": ["completed", "rejected", "failed", "human_action_required"] },
    "artifacts": { "type": "object" },
    "evidence": { "type": "object" },
    "error": {
      "type": ["object", "null"],
      "properties": {
        "code": { "type": "string" },
        "message": { "type": "string" },
        "retryable": { "type": "boolean" }
      }
    }
  }
}
