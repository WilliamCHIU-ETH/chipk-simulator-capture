# ChipK Simulator Capture Agent Guide

This repository owns the versioned process and JSON boundary for optional ChipK material acquisition. Marketing Video owns fallback policy and final video end-to-end behavior.

The contract-only preflight branch must not contain internal route data, Builder snapshots, persona metadata, credentials, runtime recordings, or a claim of production readiness.

Never store or print passwords, tokens, cookies, MFA values, recovery codes, or Keychain values. Runtime artifacts stay outside Git.
